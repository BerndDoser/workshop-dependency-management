docker run -it braintwister/ubuntu-18.04 bash

# What is installed
pip --version
pip list

# Install virtualenv
pip install --user virtualenv

# Add local module into PATH
export PATH=/home/user/.local/bin:$PATH

# Create venv
virtualenv tensorflow-1.14

# Why tensorflow is not listed?
pip list

# Enter virtenv
source ./tensorflow-1.14/bin/activate

# Install TensorFlow package
pip install tensorflow

# Leave virtenv
deactivate




# Maintain installed packages
## Print all installed packages
pip freeze > requirements



# Build wheel
docker run -i -t -v $PWD:/work quay.io/pypa/manylinux2010_x86_64 /bin/bash
